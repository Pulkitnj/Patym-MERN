import React from 'react'

function Title({text}) {
  return (
    <div>
      <div className = "text-gray-600 text-center ">
        <div >{text}</div>
      </div>
    </div>
  )
}

export default Title
